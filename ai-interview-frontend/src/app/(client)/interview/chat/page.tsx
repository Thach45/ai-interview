'use client';
import { Suspense } from 'react';
import InterviewRoomTextPage from '@/views/client/InterviewRoomText';
export default function Page() { return <Suspense><InterviewRoomTextPage /></Suspense>; }
